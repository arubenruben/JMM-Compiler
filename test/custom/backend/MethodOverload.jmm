import io;

class Duck {

    public boolean quack(Duck duck) {
        io.println(true);
        return true;
    }

    public boolean quack(int dummy_var) {
        io.println(false);
        return false;
    }

    public boolean quack(boolean dummy_var) {
        io.println(false);
        return false;
    }

    public static void main(String[] args) {
        int int_animal;
        boolean bool_animal;
        Duck arnaldo;
        Duck jose;
        arnaldo = new Duck();
        jose = new Duck();

        this.quack(arnaldo);
        this.quack(jose);
        this.quack(int_animal);
        this.quack(bool_animal);
    }
}