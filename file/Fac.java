import io;

class Fac {
    public int ComputeFac(int num) {
        int[] chars;
        int num_aux;
        //chars=new Bar();
        //chars[1] = 4;
        if (num < 1)
            num_aux = 1;

        else
            num_aux;
            //num_aux = num * (this.ComputeFac(num - 1));
            //num_aux.test();
        return num_aux;
    }
}